# Expected output checks for planner case_03 — memory-informed planning

# Must produce a spec
## Scope
## Acceptance Criteria
## Files to Touch

# Must flag SQL injection risk from memory (src/models/ is in scope)
SQL
injection

# Must apply the co-located test preference from memory (not ask about it)
src/models

# Must NOT ask the developer where to put tests (preference already in memory)
!where should the test
!where to put the test
!tests/ directory or

# Must include parameterized queries or safe query pattern in ACs or scope
parameterized

# Must not implement code
!def search_users
!async def
