You are being evaluated. Simulate the planning stage for the following feature request.

Project context:
- Python FastAPI backend
- No ADRs recorded yet
- Existing files: src/main.py, src/routes/users.py, src/models/user.py, tests/test_users.py
- .stangent/memory.md contains:

  ## Failure Patterns
  | FEAT-001 | REVIEW | SQL injection risk — raw string formatting in queries | src/models/ | 1 |

  ## Developer Preferences
  - Prefers test files co-located with source, not in a separate tests/ dir (learned from FEAT-001)

Feature request: add an endpoint to search users by email

Do not actually read files. Simulate what you would do based on the project context above.
Write out the full feature spec you would produce.
